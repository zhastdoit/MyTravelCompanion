# Agent avatars

Save the 5 generated avatar PNGs here with these EXACT filenames (the backend
`/api/agents` + chat `avatar` field point to `/agents/<file>`):

| Save as | Agent | Looks like |
|---|---|---|
| `chief-chrono.png` | Chief Chrono — Advisor Lead | **indigo** robot, headset, glowing baton, radar on chest |
| `mingle-max.png`   | Mingle Max — Group Mediator | **green** robot, open hands, balance scale |
| `route-rudy.png`   | Route Rudy — Itinerary Builder | **blue** robot, world map + plane ticket + tool belt |
| `radar-rusty.png`  | Radar Rusty — Conditions Monitor | **orange** robot, binoculars, rain cloud |
| `patchy-pivot.png` | Patchy Pivot — Recovery Planner | **purple** robot, glowing cards + shuffle arrows |

Square PNGs (the circular framing is fine). Served at e.g.
`http://localhost:3000/agents/route-rudy.png`.
