# Core (needed to run)
fastapi==0.136.3
uvicorn[standard]
pydantic>=2.7
python-dotenv
httpx>=0.27         # external API client (Amadeus / Geoapify / OpenWeather)
cachetools>=5.3     # tiny TTL cache for upstream calls
PyJWT[crypto]>=2.8  # verify Supabase user JWTs (HS256 + ES256/RS256 via JWKS)

# Real LLM mode (USE_MOCK_LLM=0)
openai>=1.40

# Optional infra — code degrades gracefully if absent
redis>=5            # HOT TripState store (else in-memory fallback)
weave               # observability/eval (set WEAVE_PROJECT to enable)
supabase>=2.6       # COLD trip persistence + auth admin

# Test deps
pytest>=8
respx>=0.21         # httpx mocking for backend tests
